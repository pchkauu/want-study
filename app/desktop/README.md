# Want Study Desktop

macOS desktop shell. Настраивает `Observatory.run`, DI, gRPC-клиенты, health gate, Git publication и facade пакета `study`.

Запуск из корня репозитория:

```shell
docker compose up -d --build
cd app/desktop && fvm flutter run -d macos
```
