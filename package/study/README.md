# study

Внутренний Flutter-пакет Want Study. Содержит версионированную доменную модель, application-сценарии, BLoC, effects и русский UI.

Публичная точка входа экспортирует `Config`, `Dependencies`, `initPackage` и facade функций приложения. Infrastructure передаётся через repository-контракты.
